{
  "name": "lifecycle: initialize reply shape + shutdown + clean exit",
  "doc": "initialize -> {manifest:$IGNORE, server_version:$ANY_STRING}, no tools advertised; shutdown -> {ok:true}; process exits 0",
  "manifest": "[plugin]\nid = \"confplugin\"\nversion = \"0.1.0\"\nname = \"Conf\"\ndescription = \"conformance fixture\"\nmin_nexo_version = \">=0.1.0\"\n",
  "fixture_config": {},
  "steps": [
    { "send": { "jsonrpc": "2.0", "id": 1, "method": "initialize", "params": {} } },
    { "expect": { "jsonrpc": "2.0", "id": 1, "result": { "manifest": "$IGNORE", "server_version": "$ANY_STRING", "tools": "$ABSENT" } } },
    { "send": { "jsonrpc": "2.0", "id": 99, "method": "shutdown", "params": { "reason": "conformance" } } },
    { "expect": { "jsonrpc": "2.0", "id": 99, "result": { "ok": true } } },
    { "expect_exit": 0 }
  ]
}
